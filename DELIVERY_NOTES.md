# ملاحظات التسليم — صرف مواد البلاغ من المخزون (المسار B)

## السلوك المنفذ

- يظهر الفني المسند للبلاغ في شاشة التسليم كمرجع ثابت غير قابل للتعديل.
- اختيار الفني المستلم فعليًا إلزامي، ويمكن اختيار الفني المسند نفسه أو فني بديل.
- صرف كمية موجبة من بند طلب الشراء يكمّل احتياج ذلك البند للبلاغ، بينما يبقى الرصيد الزائد في المخزون العام.
- لا ينتقل البلاغ إلى `received_warehouse` إلا بعد تسليم جميع البنود الفعالة المرتبطة به.
- بعد إكمال حصة البند لا يظهر اسم فني البلاغ القديم على الرصيد المتبقي.
- حركة المخزون ووثيقة التسليم تحفظان البلاغ وبند الطلب والفني المسند والمستلم الفعلي.
- الخصم وتحديث بند الطلب وإنشاء الحركة والوثيقة تتم في معاملة واحدة، مع خصم شرطي يمنع تجاوز الرصيد والتسليم المتزامن المكرر.

## قاعدة البيانات

يجب تطبيق الترحيل:

`drizzle/migrations/2026_08_04_ticket_material_delivery_link.sql`

الترحيل يضيف حقول توثيق وفهرسين إلى `delivery_documents`، ولا يحذف أو يعدّل السجلات القائمة.

## التحقق المنفذ

- نجح فحص بناء الجملة بتحويل TypeScript للملفات العشرة المعدلة.
- نجحت اختبارات مباشرة لقواعد ربط البلاغ، وإخفاء الربط عن الرصيد المتبقي، وإلزام المستلم الفعلي، وعدم فتح الإصلاح قبل اكتمال جميع الأصناف.
- نجحت اختبارات عقد التكامل الثابتة للواجهة والخادم وخدمة الصرف.
- لم تُشغّل حزمة Vitest الكاملة لأن تبعيات `node_modules` غير موجودة في نسخة المشروع المرفوعة.

## ملاحظة البيانات السابقة

هذا الإصلاح يمنع تكرار المشكلة مستقبلًا. لا يتضمن تعديل السجل التاريخي `DLV-2026-240026` أو أي بيانات سابقة؛ معالجة السجلات القديمة تتم بأوامر قاعدة بيانات منفصلة وبعد موافقة صريحة.

## Path C external maintenance workflow

1. انسخ الملفات مع الحفاظ على المسارات.
2. نفّذ ترحيل `drizzle/migrations/2026_08_04_path_c_external_maintenance_workflow.sql`.
3. أعد تشغيل الخادم وسجل الخروج والدخول.
4. اختبر: تجهيز المستودع → موافقة خروج الحراسة → تسعير واعتمادات المندوب → موافقة دخول الحراسة → استلام المستودع → التسليم لإعادة التركيب → الإغلاق.
