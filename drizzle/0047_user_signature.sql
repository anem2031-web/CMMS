-- ============================================================
-- 0047_user_signature.sql
-- التوقيع الإلكتروني للمستخدم: صورة توقيع تُرفع من صفحة المستخدمين
-- (بصلاحية admin/owner فقط) وتظهر تلقائيًا في خانة توقيع صاحبها
-- داخل وثيقة «طلب مشتريات وصرف عهدة مالية» الصادرة بعد اعتماد الحسابات.
-- تُخزَّن كمفتاح/رابط للملف المرفوع على نفس تخزين المرفقات الحالي.
-- ============================================================

ALTER TABLE `users`
  ADD COLUMN `signatureUrl` varchar(500) NULL COMMENT 'رابط صورة التوقيع الإلكتروني للمستخدم';
